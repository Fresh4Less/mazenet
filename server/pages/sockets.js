//sockets.js
var socketio = require('socket.io');
/*
module.exports.listen = function(server) {
    var io = socketio(server);

    var pagesSocket = io.of('/pages');
    pagesSocket.on('connection', function(socket) {
       // socket.on ...
    });
    return io;
};
*/