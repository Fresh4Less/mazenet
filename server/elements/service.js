var BPromise = require('bluebird');
var CustomErrors = require('../util/custom-errors');

var elements = [
	'links'	
];

function createElement(pageId, elementParams) {
		
}

function _validateNewElement(elementParams) {
	
}

module.exports = {
	createElement: createElement
};

